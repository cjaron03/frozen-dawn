# MACS Slice 6 — reconnaissance

MACS is the Maeve–Architect Cognitive System. This slice implements §§9.4–9.5 of [MACS — Source of Truth](https://www.notion.so/39d7cfaa890181c1bad4f6babad80880), on `feat/maeve-reconnaissance`, targeting `feat/maeve-director` after Slice 5 merged as `413f6ed` (PR #91).

**Live exit criterion passed on 2026-09-17.** The player described the reconnaissance Architect walking up, walking away, returning to its normal appearance, and then moving back before the scripted pause. They confirmed forming that description before reading the dump and seeing the eyes change from purple back to blue/cyan. The matching trace records inspection, an OPEN report and completed withdrawal, with no attack. The later turn back was ordinary roaming with no target. This establishes recognition of a separate non-combat mission; it is not a claim that all future behavior or visual details have been playtested.

## Behavior and knowledge

An eligible ordinary Architect that can currently see a Survival/Adventure player can receive a survey of one previously witnessed access point. The point must be in the same dimension, within 24 blocks, at least 600 overworld ticks old, and have confidence above 0.05 but below 0.75. These are recording/dispatch defaults. Unknown geometry does not create an entrance, and a current hidden target position is never an objective.

The packet freezes one entrance's inside/outside points, state, confidence and latest provenance; at most four already-observed danger points; three scored alternatives; and four orders. It contains the subject's identity, observer, encounter, dimension, issue time and 360-tick deadline. The Architect does not receive the hive database or later changes to it.

The authored alternatives are surveying the entrance, local engagement and withdrawal. Utility is tactical + survival + objective + information gain × curiosity − exposure − attention. Uncertainty supplies the expected information gain. Exposure grows with travel and a nearby or recently damaging opponent; low health favors survival. A currently eligible strong commitment favors local engagement. Difficulty changes only the existing attention capacity; it does not improve confidence, damage, speed or perception. Scores and rejected alternatives are retained with the packet.

The executor walks toward a local vantage three blocks beyond the remembered outward crossing, faces the entrance using its existing thinking pose, inspects for 100 ticks, then leaves for 200 ticks. Ordinary pursuit of that subject stays suppressed for the existing 600-tick departure window. A mission has a 360-tick deadline and an 80-tick movement-progress guard. Unsupported terrain, fire, fluids, inadequate health or an unavailable route can end it early. It uses the existing bounded walking-only D* path mode; it does not breach, scaffold, build cover or invent structures during this survey.

Actual damage interrupts the survey and returns control to local self-defense. Completed, interrupted and evicted missions delay that executor's next dispatch by 1,200 ticks. NoAI, death/removal, unload, erasure and shutdown release execution. A timeout or failed inspection does not create knowledge.

During inspection, a report describes only the short historical crossing segment that this observer can inspect from within six blocks. A visible collision in the crossing can establish BLOCKED; OPEN requires a clear view of the segment's feet/head clearance. An unrelated intervening wall yields UNSEEN. Inspection uses at most nine points with three height samples and loaded-chunk ray checks. No chunks are loaded. OPEN/BLOCKED reports carry their observer, encounter, position, time and action; they update the access point with a provisional 0.90 observation weight. They do not establish that the player prefers a retreat route or knows about a room. A changed access state records a contradiction; perception can correct either an old OPEN or BLOCKED state.

A witnessed feet cell can contain a partial supporting floor, such as a dirt path, slab or snow layer. Inspection uses the shared walking collision geometry to distinguish that floor from an obstruction, then aims above its surface within the same cell. The original three height samples and sight checks still apply. It retains the historical coordinates in the packet and cannot report an entrance hidden behind another wall.

Visible player movement during inspection uses the same existing spatial-event validator. A roaming Architect's locally selected candidate now reaches that validator even though its utility controller does not set vanilla's target field. The validator still requires the Architect's own sight. The real-AI practice regression caught this missing delivery path; no debug target lock is used in this rehearsal.

## Purple eyes

The owner initially requested exactly one visual distinction: purple eyes, with the existing blink. A synchronized transient flag enables a render layer only for the active scout and its withdrawal. It covers only the existing two 2×3-pixel eye regions, preserving the three brightness rows and the thin closed-eye row. The layer calls the renderer's existing 97-tick blink decision; it adds no timer or animation.

The owner revised the cue on 2026-09-23 after the integrated camp test. Scouting uses the existing hand-to-chin pose and empty hand. After 200 ticks of withdrawal, the body dissolves over 20 ticks into a visible soul cloud at the same physical entity. During the remaining departure interval the actor stops moving and cannot attack either the subject or another player. In the last 20 ticks of the existing 600-tick interval, the particles converge and the body returns. Ordinary targeting resumes at expiry. Distance traveled does not control this transition, so an obstructed withdrawal still has a timed conclusion.

The cloud remains vulnerable and retains the original identity, health, collision and position. Effective damage reforms it immediately for local defense. This is presentation of a noncombat pause, with no teleport, health reset, duplicate entity, delayed report or hidden attack. Purple eye shape and the existing blink remain unchanged. The synchronized dissolve value is transient; reload, erasure, death and disabled AI cannot leave an invisible active combatant. The baseline cloud emits four client-only particles per tick without consuming the AI random stream. The owner accepted this dissolution/return sequence on 2026-09-23; its trace and description are preserved under `build/macs-integrated-evidence/recon-soul-live-20260923`. That replay predates the pulse exchange below.

## Soul pulse and scout evidence calibration, 2026-09-23

The owner approved one outgoing soul pulse and a later returning pulse within the existing cloud pause. At cloud age 20–59 ticks, a small expanding ring sends a soul trail up to six blocks overhead. At age 340–379, the trail descends and a ring contracts into the cloud before reformation. A roof shortens the visual trail to the available clearance. This client-only ray stays in the loaded column and never feeds sensing or planning. The effect uses two extra particles during each pulse and twelve at each ring endpoint: at most eighteen per tick, with four during the ordinary cloud. One synchronized start timestamp anchors the exchange; no per-tick network counter or AI random draw is added. Damage, erasure, shutdown and entity reload clear its clock with the existing cue.

The exchange depicts the hive connection. It does not move reports to the cloud: accepted evidence still uploads immediately, and Maeve has no physical location in the sky. The original entity remains vulnerable and the departure deadline stays unchanged. The new pulses require a separate live visual check.

A behavioral observation made by an active mission's observer about that mission's subject now gives **0.30 supporting confidence**, compared with the ordinary **0.20**. Eligible actions are witnessed projectile damage, completed restorative use under cover and continuous outward crossings. Their original visibility, range, Survival/Adventure, final damage and completed-use requirements remain authoritative. Actual fatal hits can receive the bonus before cleanup; the mission's deadline is checked at observation time. Incidental observations of another player, withdrawal, cloud time and later combat use ordinary weight. Purple eyes alone never authorize a bonus. Direct spatial inspection keeps its separate 0.90 weight, and conditional results from withdrawal keep their ordinary weight.

Contradictions remain **−0.35**. One supporting and one contradictory contribution per pattern/player/encounter remain shared across observers and reloads. For the same recovery event, an eligible active scout wins priority among the existing maximum sixteen queried observers. A later scout cannot upgrade a contribution already scored earlier in the encounter. Three distinct scout encounters can reach 0.90 from empty history; the response still uses knowledge frozen before its encounter, so the third observation cannot enable its own immediate counter. Admission still needs a previously witnessed uncertain access point; low bow confidence alone does not summon a scout.

New provenance records retain their nominal scoring weight and, for mission observations, the mission UUID. The original credited report stays alongside at most one latest freshness verification per encounter, all within the existing eight-entry cap. Dumps label freshness-only events `VERIFY` with `weight=+0.00`. Optional `confidenceWeight` NBT retains version-4 compatibility; legacy records have unknown weight and are never relabeled as scout evidence. Counts may exceed retained history, as before.

Four required native cases cover weighted fatal damage, hidden/interrupted versus completed recovery, competing witnesses, subject isolation, departure/deadline exclusions and weighted crossings with frozen history. Unit cases cover reload, ordinary-first deduplication, retained credit, unchanged contradictions and legacy metadata. Use the same required regression fixtures and seeded stress matrix. Validate the pulse first without attacking; test the evidence adjustment in a separate encounter, so presentation and behavioral calibration remain distinguishable.

## Attention, multiplayer and persistence

Each reconnaissance mission consumes one shared focus slot and is the first eviction candidate. At most one survey may be admitted per player encounter, shared across all observers. A failed or evicted attempt still consumes it. Combat or a position commitment closes survey admission for the rest of that encounter, even after the hold ends. These two admission flags persist with the existing bounded encounter policy; a reload cannot refund the survey. A different player has a separate encounter policy. An executor also remembers recent local combat for 600 ticks, preventing a hidden pursuit from converting to reconnaissance when sight returns. A sole passive observer switching to survey reuses its own slot and begins a new 100-tick dwell. Other observers retain shared tracking; a separate survey must then gain its own slot. Lower-priority newcomers cannot displace commitments or sieges under the owner-approved Slice 5 rule. Pressure that evicts a mature scout makes it visibly leave while retaining the unresolved access point for a later dispatch.

Switching subjects releases the executor's previous passive-tracking membership before admitting a mission or commitment. Other observers of the previous player retain their slot. The required multiplayer regression covers both freeing a sole tracker's slot at capacity and refusing admission when a shared old concern still genuinely occupies capacity.

At most five missions exist per server, with 16 recent packet/result snapshots and 256 executor retry entries. Packets, executor references, recent mission diagnostics, eye flags and retry timing are transient. Local planning retries no faster than once per second, considers at most 64 points in the subject's existing bounded profile, and retains at most four danger hints. Mission cleanup runs once per second using loaded entity UUID lookups. Walking computation is bounded by the existing D* cell cap and 80 expansions per step.

Tactical SavedData remains version 4 with optional encounter admission flags: completed local reports use the existing labelled-point/provenance format. Old worlds load without mission state. Reports survive reload; in-progress packets and cues do not. Authoritative ERASED, forced erasure and debug erasure clear knowledge and execution immediately; reversing a debug override starts empty. The permanent violation ledger is unchanged. A complete external backup restores its own saved observations as before.

`MaeveDirector` remains the sole external facade, below the enforced 300-line limit. Package-private `MissionPlanner`, `MissionSensing` and `StrategySelector` own dispatch, validation and scoring. The entity controller handles local movement; the renderer and particle helper handle only presentation.

## Diagnostics and verification

Operator `/fd maeve dump [player-or-uuid]` includes active and recent missions for that subject: inherited packet, objective/orders, utility components, alternatives, report, outcome and knowledge boundaries. `/fd maeve status` includes bounded mission/attention state. The command namespace and intentional `MaeveDirector` class name remain stable. An erased dump exposes no old packet. No permanent tactical archive is created inside the world; explicit operator actor-trace exports remain QA evidence.

Required command: `./gradlew architectVerify architectMonkey --console=plain`, preserving established fixtures and seeds. Added required GameTests exercise actual autonomous survey/travel/inspection/departure, hidden versus visible obstruction, unchanged player-tendency confidence, damage interruption, shared-slot pressure, minimum dwell, isolated player knowledge, packet/report persistence boundaries, erasure, timeout, Master/NoAI exclusion, and native parsing of the live functions. Practice geometry runs real observer AI at offsets 0/7/19 through a 30-second preparation delay, a walking crossing, and five more seconds on gold. It verifies a real access-point observation, checks containment every tick, and rejects any damage without using resistance in the test.

The initial rehearsal regression failed because roaming presence never reached the existing spatial hook. Its failed log is retained at `/tmp/macs-slice6-gates.log`; the corrected required cases pass. Final delivery evidence, commit identity, test counts, fingerprints and jar hashes are retained under `build/macs-slice6-evidence/verification` and published on the PR.

The first live setup exposed an escapable two-block-high booth window: actor `80f3b67e-d78e-4ec0-8223-4fbcfcf9e481` climbed out and recorded 15 melee hits before the player froze the game. Its trace `30407f26-ac77-4d18-8874-2e013aa66b62` and client log are preserved under `build/macs-slice6-evidence/setup-failure`. The strengthened containment regression reproduced the escape at tick 45. The booth now has a single-block eye-level window, preserving real sight while preventing that climbing route. The first restart also exposed a parser-permission mismatch: GameTestServer accepts permission-3 `/tick` commands inside functions, while the integrated client's permission-2 compiler rejects them. The regression now reparses every generated function at permission 2; unfreezing is a direct player command. These failed setups are not evidence for the live reconnaissance exit criterion.

The successful replay is `712f0468-9717-4510-8ed4-13bf8ddc67d7`, actor `056b3320-a92a-47c3-bcbe-7bfa0066e0ac`, mission `a86f8f02-834e-40ce-8dcb-539eedaa530d`. A real crossing at tick 289420 supplied the packet's 0.20-confidence access at 807,101,705, with the covered sample at 805,101,705. The new Architect inherited that observation, inspected it at tick 290496, reported OPEN, and completed the survey at 290596. The resulting point retained both observers' provenance at confidence 1.00; the player's retreat belief remained 0.20. After the ten-second departure, the eye cue ended and the trace returned to ordinary FORTIFY/roaming with `target=none`. There were no recorded melee hits or block changes. The 23-second pause came from the QA function. The trace and direct pre/post dumps are retained under `build/macs-slice6-evidence/restart-replay`; the live run used unchanged production behavior from the Slice 6 client with the repaired test pack. The final diagnostic wording correction does not change that behavior.

## Executable replay

The isolated **MACS Recon Encounter** is copied from a closed QA world. The source world is preserved. Its initial setup clears tactical memory only in the disposable copy. Repeating setup does not reset observations. Open that separate save from Singleplayer before running these commands; a camp world does not run the recon progression timer. Every recon function checks for the dedicated pack's marker before changing anything, and gives a wrong-world message otherwise. Starting too early prints the current stage instead of silently doing nothing. If a new copy inherited old rehearsal scoreboard values, use `/function macs_recon:load` and `/function macs_recon:initialize` once in that disposable copy.

1. Open **MACS Recon Encounter** and run `/function macs_recon:setup`.
2. Stand on blue until the three-second prompt, then walk normally through the doorway to gold and stop briefly. The enclosed observer has real AI and sight; it cannot reach the player.
3. At “Crossing exercise complete,” run `/fd maeve dump` directly. It should retain a witnessed access point and one crossing, not injected knowledge.
4. Click the yellow skip-gap link, or run `/tick sprint 620t` once. The gap contains no active test actor; it preserves the production 600-tick age/encounter rules. Wait for both Ready and Sprint completed.
5. Run `/function macs_recon:start`. Stay near green initially, then move normally. Describe the encounter before reading another dump. It pauses at 1,100 ticks (55 seconds), after the full withdrawal and return and exports the actor trace; that final scripted pause is not a mission decision.
6. Run `/fd maeve dump` directly after describing it. Review `MAEVE_RECON_ASSIGNED`, `MAEVE_RECON_INSPECTING`, `MAEVE_RECON_REPORT`, `MAEVE_RECON_FINISHED` and extraction in the trace against the retained packet and world provenance.

For an interrupted rehearsal, run `/reload` after installing an updated pack, then `/function macs_recon:restart`. If the game is frozen, run `/tick unfreeze` directly in chat afterward. Restart exports the current actor trace, removes the failed actor, resets progression counters, restores the player's practice effects, and begins at the next unused site. It never erases accepted observations or reuses an already-observed entrance. After all three sites are used it removes the actor and stops, preserving evidence.

For a new physical site after a completed round, use `/function macs_recon:next_site`. For a fresh site whose entrance is sealed after the witnessed crossing, use `/function macs_recon:sealed`. Each adds one ordinary crossing and its skippable empty gap. Three separate sites are provided; `/function macs_recon:status` reports progression. Earlier accepted observations and traces remain intact. No function injects a belief, chooses a mission, fixes a target or commands a departure.

This slice surveys known uncertain access. It does not implement siege orchestration, destructive probing, room/storage classification, per-player conditional models, strategy-performance learning, individual traits, forward simulations or §10 capabilities.

## Integrated camp regression, 2026-09-23

The player reported repeated moving cover, followed by a mid-fight scout. The dump for actor `9c501c87-026b-4cea-854f-9729b8556ba8` confirms a commitment at tick 448707, completion at 449107, and a survey admission at 449427 in the same encounter. The old planner only excluded an active hold, allowing this conversion after expiry. The encounter combat/admission flags close that gap. The original log and all actor exports are preserved outside the save under `build/macs-integrated-evidence/cover-feedback-20260923`. This replay predates the fixes and does not validate them.

The pulse/scout follow-up passed `architectVerify architectMonkey` on 2026-09-23: 600 unit tests, 124 native regressions and the unchanged 500 seeded stress cases (624 total native cases in the stress run). The facade is 295 lines. Exact uncommitted source fingerprint, jar hash, reports and remaining live checks are recorded in `build/macs-pulse-scout-evidence/verification.json`. Both the pulse and the targeted scout-weight check passed their separate live checks below; the previously accepted base cloud is not evidence for either new change.

## Pulse live acceptance, 2026-09-23

The owner reported that the scout walked away, dissolved, sent a pulse upward, and received a later returning pulse; they said it looked good. This is informed visual acceptance, not a blind comparison. Run `a78667ce-09d1-4db8-b1c0-63eb8b18f03a` uses source fingerprint `7e32b2b9e8d0178cc9651163302e9055d70e810b5cda9cadf4166a5c1e183371` and retains its complete trace without dropped entries: mission admission at run tick 40, OPEN report at 118, survey completion at 218, dissolution at 418, reformation at 798, return to ordinary execution at 818, and an ordinary melee hit at 856. The user's report supplies the visual pulse evidence; the journal corroborates the surrounding lifecycle. The direct dump at 21:49:39 retained the historical entrance and new report. Evidence is archived at `build/macs-integrated-evidence/pulse-scout-20260923/live-pulse`. The ranged belief is still absent; the next distinct encounter can test the scout's first 0.30 contribution without erasing prior history.

## Scout bonus live acceptance, 2026-09-23

Run `ab148d40-e0a4-4611-a8d9-4cfa3b05ec96` retained all 189 journal entries without drops on the same verified source fingerprint. Mission `4020c8e5-1cdb-47ed-8e0f-cefc93a269bc` began at run tick 40 and entered inspection at 118. A real arrow hit at tick 146 / world tick 551948 earned `weight=+0.30`, ended reconnaissance with `LOCAL_DEFENSE`, and interrupted thinking. A second real arrow at tick 287 / world tick 552089 produced `VERIFY weight=+0.00` in the same encounter. The direct dump at 21:52:57 showed `PLAYER_PREFERS_RANGED confidence=0.3000 evidence=1 contradictions=0`; its credited mission observation remained intact beside the verification. The current encounter's frozen basis still contained only the earlier retreat bearing, with no new ranged commitment. An ordinary melee hit at tick 362 confirms local combat resumed.

This passes the targeted active-scout bonus, interruption and encounter-cap live check. Evidence is preserved under `build/macs-integrated-evidence/pulse-scout-20260923/live-bonus`. Broad balance and later learned-response strength remain separate calibration work.
