# MACS combined camp playtest

Current review summary: [post–Slice 7 findings and remaining integration work](macs-calibration-findings.md). The dated sections below preserve each replay and its build; commands and pending checks in older entries describe that stage of testing. The latest shield behavior and live acceptance are documented separately in [sword guard](macs-sword-guard.md).

## Cover cadence and ordinary PEEK correction (2026-09-23)

The owner accepted the noncombat scout's thinking pose in the camp. The held-bow reaction proposal was canceled. Soul-particle dissolution was subsequently reapproved and accepted in a separate recon replay; see the dated updates below. This calibration increases ordinary barrier preference using historical ranged confidence of at least 0.75 and repairs the repeated ordinary PEEK seen at the end of the earlier replay. The frozen encounter baseline, positional commitment cap/duration, scout behavior and perception rules remain in place. See [the decision rules](maeve-commitment.md) for exact weights and retry bounds.

For the next informed check, keep the existing camp and its observations. At Ready, use `/function macs_trial:enter`, `/function macs_trial:refill`, then `/function macs_trial:start`. Move around the Architect with space between you, continue ordinary ranged combat, and watch whether it uses cover again after moving. A normal PEEK should be a short look followed by another action; a Maeve position commitment can still hold for 400 ticks. Finish with `/function macs_trial:finish`, record the experience, then run `/fd maeve dump` directly in chat. Use only the empty, actor-free gap for `/tick sprint 620t` if another encounter is needed. Never reset accepted beliefs to force eligibility.

The actor journal records `FORTIFY_SELECTED` with its historical utility bias, `FORTIFY_FINISHED` with the number of blocks actually placed, and `PEEK_FINISHED`. Compare these events and actual movement against the dump's frozen confidence/cooldowns. The paired seeded utility test measures increased choices; native cases require actual construction, separated repeat attempts, movement out of the low-health corner, per-player isolation and erasure cleanup. The required fixtures and stress seeds are unchanged. Automated gates and the next live replay must be reported separately.

Slice 7 merged through PR #93 into `feat/maeve-director` at `27ef17a0f8759a00b64136e221a5c6f10d032844`. This branch prepares integration QA, not Slice 8. The [source of truth](https://www.notion.so/39d7cfaa890181c1bad4f6babad80880), especially §§9.19–9.20 and §12, governs calibration and the eventual merge to `main`.

## What Brutal changes

MACS permits five simultaneous attention concerns on Brutal and three on Normal (`default` in the command). It uses the same current confidence weights, 0.75 commitment threshold, 400-tick hold and 600-tick encounter separation. A single duel cannot demonstrate the larger budget. With one subject the current implementation permits at most one reconnaissance mission, one commitment and one shared tracking concern, so solo testing cannot establish the full attention-budget difference. A later multiplayer test supplies simultaneous concerns; adding duplicate ordinary watchers to one player does not create independent tracking slots.

The full Frozen Dawn presets also alter apocalypse duration, temperature, heating, snow, sanity and spawn multipliers. The first comparison isolates MACS: both copies use vanilla Normal combat, the same gear, dusk, a benign Phase 0 environment and disabled natural mob spawning. Maeve is activated through the real late-Phase-6 boundary before moving the atmosphere back; activation persists normally. This is not a full Brutal-survival verdict. Test endgame atmosphere, natural spawning and resource pressure separately after behavior is legible.

Brutal first follows the owner's requested order. Normal starts from a separate copy of the same closed source save, with fresh tactical memory on its first setup. Do not switch the first world's preset and call that a fresh comparison: it would inherit the learned history, damaged terrain and depleted inventory. Preset order also trains the human tester; counterbalance the order with a later tester.

## The camp and controls

`tools/prepare_macs_integrated_playtest.py` prepares **MACS Camp - Brutal** and **MACS Camp - Normal** in a separate client worktree. The completed Slice 7 world remains intact. Each copied world is disposable. Its first `setup` uses the authoritative debug erasure/reset transition to clear copied tactical state, preserves the separate violation ledger and constructs the same camp. Later `setup` calls only report state.

For a deliberate fresh start, preserve the closed save and its evidence outside the world first, then run `/function macs_trial:reset` followed by `/tick unfreeze`. Reset exports an active actor's trace, removes trial actors, clears Maeve's tactical memory through the authoritative erasure/reset path, resets round counters, rebuilds the camp, clears leftover camp arrows/items/experience, and replaces the player's inventory, health and hunger with the original kit. It retains that world's preset and the separate permanent violation ledger. Wait for **Camp ready**, then use `enter` and `start` as below. This resets learned history as well as failed routes: do not use it between rounds intended to build a habit.

The camp has a cabin with east/west openings, an exposed roofed porch, an opaque interior corner, a northwest supply shed, scattered trees and low cover. Its map is authored QA geometry with a bedrock perimeter. Architects use normal sensing, navigation, construction, damage, belief recording and selection. The fixture summons ordinary Architects without assigning a mission, target or counter. It never injects beliefs or labels the building for Maeve. Masters remain outside the experiment.

1. Open **MACS Camp - Brutal** and run `/function macs_trial:setup` once, then `/tick unfreeze` directly in chat to clear any inherited test pause. Wait for **Camp ready** while the needed chunks load and construction completes. No actor can start during preparation.
2. Run `/function macs_trial:enter`. Explore without an Architect. Find both cabin openings, the porch and the northwest shed. A simple survival objective is to move an emerald from the shed barrel into the cabin barrel.
3. From inside the camp, run `/function macs_trial:start`. One ordinary Architect arrives on alternating east/west sides using seed 1337. There is no countdown, forced combat response or automatic freeze. Play until the encounter resolves, then use `/function macs_trial:finish`.
4. Describe the encounter before reading `/fd maeve dump`. Finish exports actor traces, removes remaining tagged QA actors and returns the player to the waiting platform. Ending an active hold is an interruption; do not count it as a completed counter result.
5. During this actor-free gap, `/tick sprint 620t` is optional. Wait for **Ready** and **Sprint completed**. Enter again, use `/function macs_trial:refill` if needed, then Start. No actor is summoned by the timer, and all learned history and terrain edits persist.

`/function macs_trial:abort` immediately ends an active QA encounter. Player death also stops it; respawn is on the waiting platform and inventory is retained. Reloading during an active round aborts that round instead of silently continuing incomplete observation windows. `/function macs_trial:status` reports the round, stage, world preset and Maeve lifecycle. Stage 0 means ready, 1 means playing, 2 means an empty encounter gap, and 3 means camp preparation. Refill works only at stage 0; it restores the same iron armor, bow, sword, shield, food, restorative items and building materials, without changing tactical memory.

No invulnerability, resistance or night-vision effects are supplied. Initial setup removes inherited QA effects. Gear and vanilla difficulty are held constant for comparison. Environmental darkness can contribute to atmosphere but must not hide whether an Architect moved or built something.

## Tactics and observation order

Start with one chosen habit rather than trying to unlock every counter together. The cheaper eligible commitment wins, so strong ranged history can mask a spatial or recovery response even when those beliefs exist.

| Pass | Player approach | What to investigate afterward |
| --- | --- | --- |
| First contact | Retrieve supplies; use the route and weapon you would naturally choose. Let a distant, non-attacking visitor finish its work if safe. | What was actually witnessed? Did reconnaissance arise from a prior observed crossing? Was its purpose legible? |
| Consistent habit | Favor the bow across four distinct witnessed encounters. Use one cabin opening consistently when escaping. | Does a later encounter answer the earlier behavior? Count accepted encounters from evidence, not just rounds. |
| Break the pattern | Use the other opening; physically seal a previously used route while no Architect can see the change. Switch to melee for one encounter if testing the ranged prediction. | Does old knowledge survive until observed contradiction? Does a wrong commitment remain visible before recovery? |
| Beat the response | When cover appears, move to a clear angle and shoot during the hold. Compare later choices after two assessed losses. | Does performance change while the ranged belief remains strong? Use the high-confidence comparison proven in Slice 7. |
| Visibility controls | Finish a healing potion once in the opaque interior corner and once under the open porch with a visible observer. | Hidden consumption should add no recovery evidence; a witnessed covered use may support it. Do not infer hidden health knowledge. |
| Free play | After history exists, run several encounters without following an expected-action script. Build, retreat, pursue, hide or fight as you prefer. | Does the whole encounter feel intelligent and threatening? Can the player understand and exploit a mistake without a dump? |

Following an actual scout withdrawal in open sight can supply conditional pursuit evidence; do not chase every actor just to force that pattern. Instant combat is not automatically a failure: it can be the appropriate fallback after a losing strategy or absent evidence. Conversely, a convincing story is not proof—trace the observed action to the stored evidence.

This owner has already seen the mechanics explained, so these are informed playtests. A later uninformed tester remains necessary for the integration blind-playtest gate. Do not call hidden setup details a replacement for that tester.

## Evidence and tuning

After each encounter record: world/preset, round, chosen weapon and route, healing visibility, any building changes, the player's own description, perceived threat (0–5), fairness (0–5), readable behavior and dead time. Rate the experience before reading the dump. Missing perception or a noncombat interruption is inconclusive. An effective-damage interruption uses the observed damage trade recorded before release.

The QA pack exports the Architect's existing diagnostic journal every 200 ticks, at finish/abort and when a dying actor remains selectable. Periodic exports are capped at 48 per encounter. They do not freeze or reseed its AI. The journal is a bounded ring: keep overlapping exports and check dropped-event counts; a death's final ticks may be absent from the actor trace. Final-damage evidence and counter results still use production hooks and must be checked in the direct Maeve dump. Automatic function feedback can suppress dumps, so the player runs `/fd maeve dump` in chat. Copy evidence outside the world before any deliberate erasure test. Diagnostic exports add overhead, so this run is not a clean performance benchmark.

Compare Normal from its untouched copy using the same habit and gear protocol, then schedule multiplayer attention pressure, save/reload, erasure and performance checks on the integrated build. Normal's larger/smaller apparent tension cannot be assigned to attention without enough simultaneous concerns.

Tune only after a reproducible issue is identified, following §9.20: confidence thresholds, evidence weights, staleness/decay, then attention capacity; one variable at a time. Locked values require the matching source-of-truth decision before code changes. The initial QA fixture changed no production tuning; subsequent approved cover and scout-weight revisions are documented in the dated follow-ups. No promise is made that a scripted demonstration establishes final horror calibration.

## Verification

The required native GameTest parses every generated function at integrated-client permission level 2, resolves its function references, and checks camp fills against the vanilla volume limit. Both preset variants must be identical except their preset command. The paired worlds are copied from the same closed source and initialized independently. Required gates remain `./gradlew architectVerify architectMonkey --console=plain` with the existing seeds; live camp construction and ordinary player use must still be observed in the client.

The first integration gate exposed an older reconnaissance assertion that checked the scout's final position after its 200-tick withdrawal could already have ended. The test now measures displacement while the extraction role is active and requires no player targeting throughout that window and afterward. Ordinary roaming can change its later position. The failed report is retained alongside the final verification; that test correction changed no production movement or timing.

### Terrain regression from Brutal rounds 2–3

Those scouts accepted a mission but released it with `LOCAL_ROUTE_UNAVAILABLE` before inspecting. A dirt path has a surface 1/16 block below its neighbor, while the old observed route required identical integer foot-block heights and empty foot cells. A separate crossing observed during a jump projected an inspection point above the floor. The reported purple-to-cyan change followed the normal extraction window; retaliation on damage was also normal. These traces do not establish successful reconnaissance.

Observed walking now uses supported collision surfaces and permits fractional steps up to the existing 0.6-block walking limit. It retains the local search budget, loaded-chunk checks, hazard avoidance and prohibition on excavation/scaffolding or full-block climbs. Reconnaissance accounts for physics raising the feet before the center leaves a lower partial surface. It grounds its projected stance within two vertical cells and the existing one-block actor height bound, retaining the original witnessed position in its packet. Its route trace includes both the observed position and selected stance. Mission timing, attention and learning defaults are unchanged.

Two native regressions first reproduced the failures on the old build. With the repair, the same ordinary AI encounters must physically walk, inspect and report `SURVEY_COMPLETE/OPEN` without attacking the player or rewriting the observation. Another encounter requires inspection while standing on a partial surface. A fourth required case checks half-slab walking against magma, unsupported gaps, full-block barriers, water, fire and low ceilings. The existing danger-detour and unloaded-chunk cases remain required. A fresh live camp replay is still needed after these automated gates.

### Follow-up from the four bow encounters

The next four live rounds retained four ranged contributions at confidence 0.80. Their actor exports, reconstructed journals, direct dump and assessment are archived under `build/macs-integrated-evidence/live/after-terrain-rounds-01-04`. The east scout physically inspected and reported OPEN. Both west scouts reached inspection but returned `SURVEY_INCONCLUSIVE`: the remembered feet cell was a dirt path and the old sight ray aimed into its floor. Separately, the purple cue ended at 200 ticks while avoidance continued until 600 ticks. In rounds 3 and 4 the player shot the Architect before avoidance expired; those traces do not establish permanent target loss.

The inspector now aims into visible clearance above a partial supporting surface. Required native cases use actual witnessed crossings over dirt paths and five snow layers, then require the ordinary scout to travel, inspect and report OPEN. On the old implementation, the path case was inconclusive and the snow case incorrectly reported BLOCKED. Both also check that an intervening wall remains UNSEEN and a visible sealed crossing remains BLOCKED. This is relevant to production terrain: cargo-drop trails generate dirt paths, and the cold-world surface scanner explicitly accepts them beneath snow. The camp keeps its original floor.

The scout's purple eyes now cover the full departure avoidance interval and clear with its end or a real hit. Required regressions exercise both the natural 600-tick expiry and damage interruption, including ordinary target reacquisition and a subsequent melee hit without a further player attack. They allow the normal observation phase after reacquisition. Directed departure remains 200 ticks, avoidance remains 600 ticks, and all confidence, admission and combat defaults remain unchanged. The earlier extraction trace and gate results remain historical evidence; a live replay of the corrected cue is still required.

Round 2's five ice placements belonged to ordinary navigation after a bow hit interrupted departure, with no MACS commitment. That journal does not retain the planner's cached goal coordinates, so it cannot establish why the route chose that raised line. This patch does not claim to fix that separate observation.

The closed four-round world is backed up under `build/macs-integrated-evidence/backups/four-bow-encounters-20260923-181250`. Continue in **MACS Camp - Brutal** with `enter`, optional `refill`, and `start`; do not reset it. Its next distinct encounter is the first beginning with ranged confidence above the current commitment threshold, although local safety, range and visibility still decide whether a counter is eligible. To check natural return from scouting, remain nearby and visible without damaging it until the purple cue ends. Record the unprompted experience before the direct dump, and finish/export as usual. Fresh full-gate evidence belongs under `build/macs-integrated-evidence/scout-followup-verification`.

### Scout presentation follow-up

On build `20b5eea`, run `3a354f2d-fbec-4e65-a705-8351efe25b07` reported an entrance OPEN, completed inspection at run tick 216, and naturally resumed local targeting at tick 816. The owner initially mistook the still-purple scout's ordinary roaming and held ice for combat, then confirmed it resumed. The next presentation repair therefore uses the existing thinking pose and empty main hand during actual noncombat scouting and departure. A separate synchronized flag prevents this presentation from concealing combat against a different player. It changes neither target selection nor departure duration. Required cases cover travel, inspection, both departure intervals, actual melee after expiry/damage, reload/erasure, and another player's combat during the original subject's avoidance. The original client stays on `20b5eea` while this change is built in an isolated checkout.

The direct dump from that same run confirms a `HOLD_RANGED_COVER` commitment based on the four earlier bow observations at historical confidence 0.80, from world tick 414866 to 415266. It recorded a positive damage trade during the hold. The brief later purple switch was a second mission for the west entrance; its local route was unavailable, and a subsequent player hit ended departure. Neither a mission assignment nor a color change proves the route was inspected.

The final stationary interval was a separate ordinary `PEEK`, from run tick 2236 through the last export at 2680 (22.2 seconds), after the learned hold had ended. The ordinary peek executor resets its 30-tick counter and requests reevaluation, which can select the same action again indefinitely. This is a retained behavior finding, not a Maeve hold or evidence that the new scout cue failed. The presentation change does not alter that local decision policy. Raw evidence remains in the original worktree under `build/macs-integrated-evidence/live/scout-cue-round-05-in-progress`; the final export and direct dump should be retained with the completed assessment before further calibration.

### Combat and scout follow-up, 2026-09-23

The repeated-wall camp replay confirmed cadence but exposed mid-fight reconnaissance. A player encounter now admits at most one survey, before local combat or any positional bet; its admission and combat flags persist across reload. A completed scout walks away for 200 ticks, dissolves into a vulnerable stationary soul cloud, and reforms during the end of the existing 600-tick departure interval. The normal executor then resumes without a second initial observation phase. Incoming effective damage cancels either a thought/position hold or the scout pause after recording its evidence and counter result. It preserves the spent bet and future cooldown. Masters remain independent.

Cover placement now requires a grounded wall that screens the Architect's torso from the visible firing side. A historical position commitment still wagers against the inherited direction, while ordinary moving cover uses local visible geometry. Neither reads incoming arrows or the player's last-second bow input. The previous confidence coefficients, six-second construction spacing, movement requirement and six-block pool are unchanged.

Live verification after installing the update:

- In **MACS Camp - Brutal**, run `/function macs_trial:enter`, `/function macs_trial:refill`, then `/function macs_trial:start`. Shoot from the wall's protected side, then flank it. If the Architect holds its thinking pose, land an actual damaging hit and observe whether it immediately resumes combat. No scout should appear after combat begins. Finish with `/function macs_trial:finish` and `/fd maeve dump`. Do not reset the camp's accepted history.
- In the separate **MACS Recon Encounter** copy, run `/function macs_recon:load`, then `/function macs_recon:initialize`. This initializes only that disposable rehearsal. Wait for the prompt and walk blue through the doorway to gold. At Crossing exercise complete, run `/tick sprint 620t`; after Sprint completed and Ready, run `/function macs_recon:start`. Watch without hitting it through the initial inspection, ten-second withdrawal, soul-cloud pause and return. The fixture now pauses at 55 seconds, allowing the complete production sequence. Run `/fd maeve dump` afterward. A second fresh site can test interrupting the cloud with a hit.

Native verification checks actual projectile collision, final damage before hold release, zero-damage non-interruption, one survey across observers/reload, combat admission, identity/health during the cloud, retaliation, erasure and entity reload. Automated timing and collision checks do not establish that the visual sequence is readable; that remains a client replay requirement.

## Pulse and scout weight follow-up, 2026-09-23

The accepted base cloud replay is preserved under `build/macs-integrated-evidence/recon-soul-live-20260923`. Its user description and trace validate dissolution and natural return, not the subsequently added pulses or evidence bonus. The follow-up adds a bounded outgoing/returning soul exchange and active-scout support of 0.30; see [the exact constraints](macs-reconnaissance.md#soul-pulse-and-scout-evidence-calibration-2026-09-23).

Continue in **MACS Recon Encounter**, using `/function macs_recon:restart` to restore Survival and begin at the next unused site without erasing accepted history. Unfreeze if necessary, follow blue → doorway → gold, skip only the empty 620-tick gap, then start. Watch the first round without attacking through the complete cloud and return. Record the visual result before `/fd maeve dump`. A later separate encounter can test a visible bow hit while the scout has an active mission: the projectile earns 0.30 once and interrupts reconnaissance immediately; subsequent hits in the same encounter add zero. A hit during withdrawal/cloud earns ordinary 0.20. This exercise preserves production timing and does not manufacture beliefs.

## Ground scaffolding follow-up, 2026-09-23

The fresh Brutal camp's fifth bow encounter inherited 0.80 ranged confidence, selected `HOLD_RANGED_COVER`, and recorded a failed counter when the player's arrow interrupted the hold. Confidence reached 1.00 from five encounters. The owner rated threat approximately 2/5, then reported unnecessary ice underfoot on usable ground. The trace shows one upward scaffold at `(2037,101,2052)`, four westward bridge blocks, and a return to ground. All 674 events are covered by overlapping exports under the playtest worktree's `build/macs-integrated-evidence/broader-20260923/brutal-round-05`. This is informed feedback, not the integration blind-test gate.

A native regression reproduces a retained-route failure: the player moves from behind a wall onto visible open ground within the ordinary goal-reuse tolerance. Before the fix, the actor still follows the former goal and places two scaffold blocks. A fresh stationary ground route and a moving ground pursuit beside the camp wall both already pass. The original live journal did not record cached goal coordinates, so the reproduction establishes a concrete failure mechanism without proving the exact cached goal in that replay.

Before dispatching `SCAFFOLD_UP` or `SCAFFOLD_BRIDGE`, the approach controller now refreshes a changed goal using its existing local target and bounded planning budget, then selects the resulting step. Construction remains available when the refreshed route requires it. Ordinary walking retains its existing goal tolerance. The journal includes the cached goal on `PLAN_STEP` and records `CONSTRUCTION_TARGET_MOVED` when this refresh occurs. This changes neither Maeve belief tuning nor commitment timing.

The three ground-route cases are in the required manifest. Retain the failing baseline and final gate reports under `build/macs-scaffold-evidence`. The baseline also exposed one failure of the existing two-player recovery-training fixture; preserve that report alongside the final results rather than attributing it to the navigation change.

The five-round camp was closed and hash-verified into `build/macs-integrated-evidence/backups/before-scaffold-fix-20260923-221938` in the playtest worktree. For live confirmation, continue that camp without resetting: skip the empty gap with `/tick sprint 620t`, wait for Ready and Sprint completed, then `macs_trial:enter`, `macs_trial:refill`, and `macs_trial:start`. Move through the west doorway and retreat onto clear ground while using the bow. Check whether pursuit stays grounded; distinguish underfoot steps from upright protective cover. Finish, describe the result, and run `/fd maeve dump`. The subsequent round 6 replay confirms zero scaffold placements and four successful protective walls (eight blocks). At run tick 1209, `CONSTRUCTION_TARGET_MOVED` refreshed the old elevated goal `(2031,102,2048)` to the current ground target `(2028,101,2051)`. The owner confirmed only defensive pillars were built. All 626 events are covered by nine overlapping exports, archived with the direct dump and report under `build/macs-integrated-evidence/broader-20260923/brutal-round-06`. This closes the live check for the reported navigation symptom.

Round 6 also explains continued reconnaissance: the inherited bow belief was 1.00, while the remembered west crossing remained 0.20. At admission during ordinary `OBSERVE`, survey and local engagement both scored 1.30; the deterministic action-name tie-break chose survey. Combat and the first received arrow occurred after the scout returned. The survey was inconclusive again, as in rounds 2 and 4, so that access point remains uncertain. Diagnose this repeated inspection failure before further balance calibration. No position commitment was issued this round; ordinary cover received the historical ranged bias. No new threat rating was supplied.


## West doorway survey follow-up, 2026-09-23

Rounds 2, 4 and 6 all reached inspection of the same remembered west crossing and ended `SURVEY_INCONCLUSIVE/UNREPORTED`. The packet retained outside `(2036,100,2048)` on a dirt path and inside `(2039,101,2048)` above a full plank floor. Interpolating these integer feet cells places an intermediate sample inside the plank at `(2038,100,2048)`. Its low sight ray strikes the preceding path block, preventing an OPEN report. A translated native camp fixture reproduces this through actual crossing events, autonomous scout movement and mission completion; its sealed-door control already passes on the old build.

Inspection now raises an intermediate sample through a full supporting block only while below the higher witnessed feet coordinate. It then applies the existing local visibility and obstruction checks to the clearance above the floor. This does not change the saved observation, survey range, timeout, confidence weights, admission scores or encounter limits. A full block at the witnessed upper feet height remains an obstruction. The required camp regressions cover the open mixed floor, sealed doorway, reverse direction, single-block obstruction and a nearer wall hiding the entire crossing. Existing path, snow, terrain-safety and unloaded-chunk coverage remains required.

The closed six-round camp is preserved in the playtest worktree under `build/macs-integrated-evidence/backups/before-west-doorway-fix-20260923-224300`. Baseline and final automated evidence belong to `build/macs-west-survey-evidence` in the implementation worktree. The baseline completes 129 native cases with exactly one failure: the open mixed-floor survey. After the repair, `./gradlew architectVerify architectMonkey --console=plain` passes 600 unit tests, 129 native cases (124 required manifest entries), and all 500 unchanged stress cases (629 cases in that run). The fixed source fingerprint is `85576b0187aadc6dca67b727bb2c7b4b4b27639166209d9531c5c3a03c6789a4`.

The first live attempt on the repaired build (round 7, `954d9933-a9b6-43b4-b82c-9b404354124c`) did not exercise inspection: ordinary `FORTIFY` began at run tick 240, followed by `COMBAT_ALREADY_STARTED`, and no reconnaissance packet was issued. The later bow hold recorded a second failed counter. The new defensive wall occupied `(2038,101,2048)` and the actor dug the path at `(2037,100,2048)`. All 335 events and the direct dump are archived under the playtest worktree's `build/macs-integrated-evidence/broader-20260923/brutal-round-07`. Merely selecting a west arrival was insufficient to isolate reconnaissance admission; this replay is inconclusive for the sensing fix.

For the focused retry, reuse **MACS Camp - Brutal** at Ready, run `/reload`, then `/function macs_trial:survey_check`. This guarded QA function resupplies the player, restores the original west opening and the one removed path block, places the ordinary Architect directly at the remembered crossing, and places the player twelve blocks west. Its ordinary admission rules choose whether to survey; no beliefs, confidence, mission deadlines or production settings are changed. Stay at the starting position without attacking. Once the scout finishes thinking at the doorway and starts withdrawing, use `/function macs_trial:finish` and run `/fd maeve dump` directly in chat. Inspect the retained mission for `SURVEY_COMPLETE report=OPEN` and `RECON_INSPECTED_ACCESS_OPEN` evidence. This is a controlled inspection check rather than broad combat calibration.

The focused live retry passed in round 8 (`d446ae2a-8488-4b87-8e8d-a17843f91cdb`). The ordinary admission selected the old west crossing at run tick 40, the scout reached inspection and reported OPEN at tick 55, and completed the full 100-tick inspection at tick 155 before withdrawing. The direct chat dump retained `SURVEY_COMPLETE report=OPEN` and `RECON_INSPECTED_ACCESS_OPEN` at game tick 504270. That exact access record rose from 0.20 to 1.00 confidence with two evidence entries and no contradictions; the bow belief stayed at 1.00 with seven contributions. No combat, damage or block destruction occurred. Two complete exports and the dump are archived in the playtest worktree under `build/macs-integrated-evidence/broader-20260923/brutal-round-08`. This closes the live check for the mixed-floor inspection repair. The adjacent west access record at `(2036,100,2047)` and eastern records remain separate low-confidence survey candidates. This replay does not establish a blind gameplay impression or broad balance.


## Bow-combat comparison after the survey repair

Ordinary round 9 (`f073dc5e-1c2f-48b8-8264-242cb564c94f`) was rated threat 0/5: the owner reported a scout, a bow hit, and otherwise normal Architect behavior. Their original “fairness 0” was clarified as almost no threat; fairness remains unrated. The east survey completed OPEN at run tick 162 before the arrow at 224 interrupted withdrawal. The surveyed east access reached confidence 1.00. The second deferred encounter retained ranged confidence 1.00, eight ranged contributions, two failed counters, effectiveness 0.25 and `deferred=true`. It issued no learned hold and recorded no fortification or successful melee hits. Four `inWall` damage events during retreat are preserved as a separate terrain finding, with cause unresolved. All 1,040 events are covered by six overlapping exports under `build/macs-integrated-evidence/broader-20260923/brutal-round-09` in the playtest worktree. This is not a threat/balance pass. The next comparison retains ordinary bow combat; after the two deferred encounters, the counter becomes eligible for reconsideration at reduced effectiveness. Report actual selection and player perception rather than assuming a specific counter must trigger.

Ordinary round 10 (`f853137f-a9ea-4e74-96c3-caa71d0411b3`) was rated roughly 2/5 threat: “it started defending itself with blocks.” The two-encounter counter deferral had ended (`deferred=false`), while ranged confidence remained 1.00 with nine contributions and counter effectiveness remained 0.25. The recorded historical fortify bias was 0.525. Three fortify attempts produced two successful two-block walls; the first attempt placed nothing. A separate ordinary retreat built one healing wall, and the Architect later landed two melee hits. No scout, stationary commitment, navigation scaffold or `inWall` damage occurred. The commitment diagnostic remained `NO_ELIGIBLE_SAFE_POSITION`, so this was learned bias applied to ordinary cover selection, not a newly successful hold. All 700 events across five overlapping exports, the direct dump and the approximate threat rating are archived under `build/macs-integrated-evidence/broader-20260923/brutal-round-10` in the playtest worktree.

At tick 327, the pursuit planner destroyed the upper block at `(2068,102,2037)` of the defensive wall placed at tick 310. Preserve that cover/navigation interaction for review; the trace alone cannot determine the wall's net tactical value. Round 9's retreat terrain damage remains a separate unresolved finding. These two ordinary rounds differ in arrival side, player movement, retained terrain and duration, so the 0-to-2 perceived threat change cannot be attributed solely to the cooldown. The comparison establishes restored learned cover preference and modest perceived pressure, not broad balance or horror calibration. Clarify whether the walls forced repositioning or delayed shots before increasing cover frequency or prescribing further repetitions. Fairness remains unrated.

The owner clarified that the walls create uncertainty and sometimes block arrows. Record this as perceived useful defense, with threat still approximately 2/5. Exact projectile interceptions are not instrumented in the retained actor trace. The next focused gameplay question is whether flanking a new wall prompts useful repositioning and renewed pressure, or exposes self-obstruction; that should guide any later tuning.

The follow-up round 11 (`77763ee0-b3c4-453c-ab15-24e3f3a28b02`) elicited a tentative qualitative report of fear: the owner noticed the Architect trying to get around the wall to attack. No numerical rating was supplied. The first successful wall at `(2067,101,2054)` was placed at run tick 168. The actor moved along its east side through `(2068,101,2053)` and `(2068,101,2054)`, reaching `(2068,101,2055)` and a melee hit at tick 245. Overall, four fortify attempts produced two two-block walls; ordinary retreat added one healing wall, and pursuit produced five melee-hit events. Historical cover bias remained 0.525 and ranged confidence remained 1.00. No scout, stationary commitment, navigation scaffold, block destruction or `inWall` damage occurred. All 879 events across six overlapping exports, the dump and the unprompted qualitative report are retained under `build/macs-integrated-evidence/broader-20260923/brutal-round-11` in the playtest worktree.

This supports the intended combination of historically biased cover and existing local pursuit producing perceived pressure. It does not establish a separately learned flanking maneuver. Eight contact-breach attempts around the first wall were released, and two short obstruction pauses occurred before pursuit continued; one pause was interrupted by a bow hit. Preserve that inefficiency for review without ignoring the successful route around cover. Keep the prior approximate 2/5 rating attached to round 10, and leave round 11's numeric threat and fairness fields empty. Current evidence supports retaining cover tuning while extending testing beyond repeated bow encounters.

## Melee switch after the bow encounters

After round 11, the owner authorized changing combat habits to test revision of the bow prediction. Preserve the same Brutal camp, build and learning. Use the existing `enter`, `refill` and `start` commands, the iron sword in slot 2, and ordinary shield/movement/healing as needed. Attack with the sword only, ensuring visible direct damage; holding the sword alone supplies no evidence. Finish after the fight ends or roughly 45 seconds, report the experience before reading the direct `/fd maeve dump`, and inspect that first result before requesting the second sword encounter. Sprint only the empty gap if needed. The detailed prospective protocol is retained under `build/macs-integrated-evidence/broader-20260923/melee-switch-plan.json` in the playtest worktree.

The baseline is ranged confidence 1.00, ten supporting contributions and no contradictions. With qualifying melee and no arrows, one contradiction in each separate encounter should produce live confidence 0.65 then 0.30. Repeated hits within one encounter cannot accelerate this. The first encounter inherits the earlier 1.00 baseline; the second inherits 0.65 and the prior contradiction cooldown. Check historical utility and observed provenance rather than assuming every wall disappears: weak historical cover preference and ordinary retreat cover remain available. Direct melee also changes local distance and action selection, so wall counts alone cannot demonstrate learning. This checks revision of the ranged hypothesis, not a new anti-sword strategy. No live melee-switch result has yet been collected.

The first sword round (round 12, `e94f5300-e4be-408f-ab15-aa53d19bcbc0`) ended in player death after 590 ticks. The owner described it as much easier but still dangerous despite the pillars, without supplying a numeric rating. Three received player-melee damage events and no arrow damage produced one `WITNESSED_MELEE_DAMAGE` contradiction at game tick 534762. Both post-death chat dumps retained ranged confidence 0.65, ten supporting contributions and one contradiction. The inherited baseline remained 1.00 and fortify bias remained 0.525; this is the expected lagged first encounter, not the later response to reduced confidence. The Architect recorded seven successful melee hits including the fatal hit, no scout or learned hold, and no navigation scaffold, destroyed block or `inWall` damage. All 375 events, three complete exports, the death log, both dumps and the owner report are archived under `build/macs-integrated-evidence/broader-20260923/brutal-round-12` in the playtest worktree.

The first state sample, before any recorded hit, shows player health 7.16 (about 3.6 hearts). No refill was logged before this encounter, so the death cannot be used as a matched difficulty comparison. Use the existing refill command and confirm full health before the second sword-only encounter. This result verifies contradiction recording and persistence through player death; lower-confidence behavioral carryover remains the next live check.

The second sword round (round 13, `caf2f98b-916f-41df-8d4a-947d3ed3fe38`) inherited ranged confidence 0.65 and recorded a second melee contradiction, lowering live confidence to 0.30 while supporting contributions stayed at ten. Four player-melee damage events and no arrows were retained; only the first melee contribution changed confidence. The actor selected no ordinary `FORTIFY` action and issued no position commitment. Its weak cover bias is calculated as 0.195 from the frozen baseline and existing effectiveness multiplier; no fortify selection occurred to journal that value directly. Five melee-hit events, zero navigation scaffolds, zero destroyed blocks and zero `inWall` damage were recorded. The owner reported no barricades and hitting a scout. The server log separately records three ordinary retreat walls at `(2029,101,2042)`, `(2029,101,2041)` and `(2030,101,2040)` before healing, so this is absence of bow-fortify actions rather than absence of all defensive construction. The earliest post-survey player sample shows full health before the first recorded enemy hit.

The scout was assigned at tick 40 to a distinct eastern access record, outside `(2064,101,2048)` and inside `(2062,101,2048)`, with inherited access confidence 0.20. It reported OPEN at tick 68, then the player's sword hit interrupted active inspection at tick 97 with `LOCAL_DEFENSE`. This mission was about crossing uncertainty; it does not indicate a new sword model or an omniscient reaction to weapon selection. All 262 events, four complete exports, the dump and the report are archived under `build/macs-integrated-evidence/broader-20260923/brutal-round-13` in the playtest worktree. The two-round check is complete for observed contradiction scoring and transfer of the reduced belief into the following encounter. Behavior is consistent with the reduced bow bias, but movement, range, scouting and retained terrain prevent assigning the wall-count difference solely to learning. Broader balance and the earlier terrain/cover findings remain separate.
